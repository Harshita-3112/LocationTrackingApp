import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  textStyle: {
    color: '#121212',
    fontWeight: '600',
    fontSize: 14,
    left: 10,
  },
});

export default style;
